# 開発用Dockerfile
FROM node:18-alpine

WORKDIR /app

# 依存関係をコピーしてインストール
COPY frontend/package*.json ./
RUN npm install

# ソースコードをコピー
COPY frontend/ ./

EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]

